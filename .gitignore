# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
.env.*
!.env.example
# Google service-account keys (never commit)
service-account*.json
*-service-account.json
credentials*.json

# uv — the lockfile IS committed (CI installs with `uv sync --frozen`); only
# local uv state is ignored.
.uv-cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Logs
*.log
